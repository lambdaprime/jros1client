// Requires gradle 7.2
// gradle clean build
// gradle publishToMavenLocal

allprojects {
    repositories {
        mavenCentral()
        mavenLocal()
    }
}

ext {
    releaseFileName = "${projectName}-${version}.zip"
}
